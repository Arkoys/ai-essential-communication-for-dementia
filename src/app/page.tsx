// Home route — renders the existing Vite App component, now as a Client Component.
// Replace with a full Next.js-native shell once Phase 1 is finished.
import App from '@/src/App';

export default function HomePage() {
  return <App />;
}
